/**
 * Zod schemas enforcing structured JSON tool outputs
 *
 * Status: skeleton — implementation pending.
 */
export function notImplemented(): never {
  throw new Error("toolSchemas: not yet implemented");
}
