/**
 * Isolated child_process execution harness with timeouts
 *
 * Status: skeleton — implementation pending.
 */
export function notImplemented(): never {
  throw new Error("processRunner: not yet implemented");
}
