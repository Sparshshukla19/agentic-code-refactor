/**
 * Run jest/vitest and capture failures
 *
 * Status: skeleton — implementation pending.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import type { TestFailure } from "../types/verification.types.js";

export function notImplemented(): never {
  throw new Error("testRunner: not yet implemented");
}
